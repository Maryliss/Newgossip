
<div class="container text-center">
<p>Voici notre tableau de gossips</p>

<div class="list-group">
  <a href="#" class="list-group-item list-group-item-action active">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1"> #{@gossip.first.title} </h5>
      <small>#{@gossip.first.user}</small>
    </div>
    <p class="mb-1">#{@gossip.first.content}</p>
    <small>#{@gossip.first.created_at}</small>
  </a>
</div>


